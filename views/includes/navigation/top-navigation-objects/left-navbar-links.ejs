<!-- Left navbar links -->
<ul class="navbar-nav">
    <li class="nav-item">
        <a class="nav-link" data-widget="pushmenu" href="#"><i class="fas fa-bars"></i></a>
    </li>
    
    <% if(appStructure) {
        if(appStructure.navBar.length >=0) { 
            appStructure.navBar.forEach(page => { %>
                <%- include('../nav-item', {name: page.name, url:page.url, status:page.status}) %>
            <% })
            }
        } %>

</ul>